require("dotenv").config();
var keys = require("./keys.js");

var spotify = new Spotify(keys.spotify);

// get user input
// create a variable for the command
// create a variable for the data

// if user types concert this 
    // do something
// if the user types spotify this 
    // do something
// if user types movie this 
    // do something
// if the user types do what it says 
    // do something
